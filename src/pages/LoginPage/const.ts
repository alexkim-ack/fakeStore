const MINUTE = 60;
const TIMEOUT = 60;
export const MAX_AGE = MINUTE * TIMEOUT;

export const USERNAME = "Username:";
export const PASSWORD = "Password:";
export const SUBMIT = "Submit";

export type Inputs = {
    username: string;
    password: string;
};

export const ERROR_MESSAGE = "This field is required!";
